import React from "react";

import Spinner from "components/spinner";

const FullpageSpinenr = () => {
  return (
    <div className={"flex h-96 items-center justify-center"}>
      <Spinner size="large" />
    </div>
  );
};

export default FullpageSpinenr;
